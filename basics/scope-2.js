// Global (name)
    // Local (name)
        // Local (name)
    // Local

let name = "Robert"

if (true) {
    let name = "Mike"
    if (true) {
        let name = "Jacob" 
        console.log(name)
    }
}

if (true) {
    console.log(name)
}

